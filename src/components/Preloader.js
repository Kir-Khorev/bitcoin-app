import React from 'react';
import preloader from '../assets/img/preloader.gif'

const Preloader = (props) => {
    return <img src={preloader} alt='preloader' style={{ width: 'auto' }} />
}

export default Preloader;